{
  "id": "pca",
  "title": "Principal component analysis",
  "seed": 240510,
  "family": "Multivariate exploration",
  "plot_engine": "base R",
  "packages": [
    "ragg",
    "jsonlite"
  ],
  "code_simulate": "set.seed(240510)\necotype <- rep(c(\"Forest\", \"Grassland\", \"Dune\"), each=25)\nsize <- rnorm(75, rep(c(12,14,13),each=25), 1.2)\nd <- data.frame(ecotype, body_mm=size,\n  wing_mm=1.3*size+rnorm(75,0,1), femur_mm=.45*size+rnorm(75,0,.4),\n  antenna_mm=.7*size+rep(c(0,1,-1),each=25)+rnorm(75,0,.5))\nwrite.csv(d, file.path(out_dir, \"data.csv\"), row.names = FALSE)",
  "code_plot": "coordinates <- result$x[,1:2]\ncolours <- c(\"#146A85\", \"#C56B3B\", \"#5B8266\")[match(d$ecotype,c(\"Forest\",\"Grassland\",\"Dune\"))]\nplot(coordinates, pch=21, bg=colours, cex=1.1,\n     xlab=\"PC1\", ylab=\"PC2\")\nlegend(\"topright\", c(\"Forest\",\"Grassland\",\"Dune\"), pch=21,\n       pt.bg=c(\"#146A85\", \"#C56B3B\", \"#5B8266\"), bty=\"n\")",
  "code_analysis": "z <- d[c(\"body_mm\",\"wing_mm\",\"femur_mm\",\"antenna_mm\")]\nresult <- prcomp(z, center=TRUE, scale.=TRUE)\nvariance <- result$sdev^2/sum(result$sdev^2)",
  "biology": "Four correlated morphological traits are measured in beetles from three ecotypes.",
  "design": "25 independent beetles per ecotype; four traits on each individual.",
  "data_characteristics": [
    "Several quantitative variables per individual",
    "Correlated traits with different scales",
    "Ecotype labels are known"
  ],
  "other_examples": [
    "Plant functional traits",
    "Metabolite profiles",
    "Multivariate physiology"
  ],
  "question": "Which combinations of traits summarize the most variation?",
  "null": "The target is a set of linear trait combinations that explain the most variance; PCA itself does not test a null hypothesis.",
  "options": [
    "prcomp(..., center=TRUE, scale.=TRUE)",
    "Scaling gives traits comparable weight.",
    "Inspect scores, loadings and variance explained."
  ],
  "assumptions": [
    "Rows are comparable observations.",
    "Scaling and linear combinations suit the question."
  ],
  "parametric": [
    "Use a model that directly tests the biological group contrast if inference is the aim."
  ],
  "nonparametric": [
    "Consider rank- or distance-based approaches when their target matches the question."
  ],
  "generating_model": "75 beetles: 25 per ecotype. Body size is Normal(mean 12,14,13 mm by Forest,Grassland,Dune; SD 1.2). Wing=1.3×size+Normal(0,1); femur=0.45×size+Normal(0,0.4); antenna=0.7×size+ecotype shift(0,1,−1)+Normal(0,0.5). All errors independent.",
  "generating_truth": "Groups differ in body-size means and antenna shifts; all traits share a common body-size component.",
  "columns": [
    {
      "name": "ecotype",
      "meaning": "Known source habitat",
      "unit": "category"
    },
    {
      "name": "body_mm",
      "meaning": "body",
      "unit": "mm"
    },
    {
      "name": "wing_mm",
      "meaning": "wing",
      "unit": "mm"
    },
    {
      "name": "femur_mm",
      "meaning": "femur",
      "unit": "mm"
    },
    {
      "name": "antenna_mm",
      "meaning": "antenna",
      "unit": "mm"
    }
  ],
  "teaching_point": "PCA uses the measurements, not class labels, to construct the axes.",
  "plot_args": [
    {
      "arg": "pch=21 / bg",
      "note": "Shows known ecotypes with outlined colors"
    },
    {
      "arg": "xlab / ylab",
      "note": "Names the coordinates actually plotted"
    },
    {
      "arg": "legend",
      "note": "Keeps ecotype labels explicit"
    }
  ],
  "practice": "What biological claim would this plot alone fail to establish?"
}
