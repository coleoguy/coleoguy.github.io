---
title: "Control Structures in R"
author: "Heath Blackmon"
date: '`r Sys.Date()`'
output: ioslides_presentation
---

## Control Structures

`for()`

`if()`

`while()`

## Challenge

load the cool iris dataset:
`data(iris)`
and use a `for` loop and an `if` statement to calculate the average petal w/l ratio.
