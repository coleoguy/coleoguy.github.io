dat <- read.csv("bettafish.csv")
# with this data make a presentation ready plot
# that demonstrates the impact of at least two 
# of the predictor variables (sex, size, and color)
# on the repsonse variable (price).


